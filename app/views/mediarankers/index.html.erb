<h1>Top 10 Movies</h1>
  <% @movies.limit(10).each do |movie| %>
      <h2><%= link_to(movie.name, movie_path(movie[:id])) %></h2>
      <h3>Votes: <%=movie.votes.to_s%></h3>
  <%end %>
  <p><%=link_to("See more movies...", movies_path)%></p>

<h1>Top 10 Books</h1>

  <% @books.limit(10).each do |book| %>
      <h2><%= link_to(book.name, book_path(book[:id])) %></h2>
      <h3>Votes: <%=book.votes.to_s%></h3>
  <%end %>

  <p><%=link_to("See more books...", books_path)%></p>

<h1>Top 10 Albums</h1>
  <p><%=link_to("See more albums...", albums_path)%></p>
<!-- <p>Find me in app/views/mediarankers/index.html.erb</p> -->
